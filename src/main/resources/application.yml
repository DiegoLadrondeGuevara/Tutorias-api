server:
  port: 8080

spring:
  datasource:
    url: jdbc:mariadb://db:3306/tutoria_db
    username: root
    password: rootpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect
