server:
  port: 8080

spring:
  datasource:
    url: jdbc:mariadb://3.80.235.23:3306/tutoria_db  # <-- Cambia "db" por la IP real
    username: root
    password: rootpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect
